(function executeRule(current, previous /*null when async*/ ) {

    gs.info('Password Changed: for ' + current.name + ', by ' + gs.getUserID());
    current.u_password_last_change = new GlideDateTime();

})(current, previous);
