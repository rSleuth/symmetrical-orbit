function onLoad() {
    //Type appropriate comment here, and begin script below
    if (g_scratchpad.approval_level != "" && g_form.isNewRecord())
        g_form.setValue("u_approval_level", g_scratchpad.approval_level);
}
